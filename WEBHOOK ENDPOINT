app.post("/webhook", (req, res) => {
  const { from, message } = req.body;
  let reply = "";

  if (message === "1") {
    reply = "📝 Please reply with your FULL NAME to register.";
    return res.json({ reply });
  }

  // If message is a name (not number)
  if (message && isNaN(message)) {
    registerMember(from, message, (response) => {
      res.json({ reply: response });
    });
    return;
  }

  reply =
    "👋 Welcome to SACCO Assist\nReply with:\n1️⃣ Join SACCO\n2️⃣ Check Balance\n3️⃣ Save Money\n4️⃣ Help";

  res.json({ reply });
});
